# Copier en `.env` (NON versionné) et remplir avant le premier `docker compose up`.
#   cp .env.example .env

# Mot de passe du compte admin Grafana (login : admin).
# Générer un secret solide, p. ex. : openssl rand -base64 24
GF_SECURITY_ADMIN_PASSWORD=change-me
